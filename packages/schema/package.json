{
  "name": "@reactive-resume/schema",
  "version": "0.0.1",
  "private": true,
  "type": "commonjs",
  "main": "./dist/index.js",
  "typings": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "lint": "pnpm lint:prettier && pnpm lint:eslint",
    "lint:eslint": "eslint . --ext --fix .ts",
    "lint:prettier": "prettier --check \"src/**/*.ts\"",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.22.3"
  },
  "devDependencies": {
    "eslint": "^8.22.0",
    "typescript": "4.7.4"
  }
}
