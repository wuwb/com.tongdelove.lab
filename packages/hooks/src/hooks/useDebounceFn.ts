import { useDelayAction } from './useDelayAction'

/**
 * Hooks for debounce action for example search
 * @example
 *
 * const debounceFn = useDebounceFn(() => { ... }, ms);
 */
export const useDebounceFn = useDelayAction
