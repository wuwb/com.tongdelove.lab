# 2026-09-15

## admin 依赖升级（保守升级，跳过破坏性大版本）

- 已升级（package.json）：`config` 3→5、`cron` 3→4、`immer` 10→11、`immutable` 4→5、`pino` 9→10、`react-konva` 18→19、`web-vitals` 3→6、`@types/node` 24→26、`konva` 9→10、`less-loader` 12→13、`lint-staged` 15→17、`prettier-plugin-organize-imports` 3→4、`prettier-plugin-packagejson` 2→3。
- 唯一代码适配：`src/plugins/reportWebVitals.ts`，web-vitals v6 移除了 `ReportHandler` / `getCLS|getFID|...`，改为 `ReportCallback` 与 `onCLS|onINP|onFCP|onLCP|onTTFB`。
- 验证：安装用 `pnpm install --filter admin --prefer-offline`（不要无 filter 跑全量）；`tsc --noEmit` 532 errors / 163 files，与升级前完全一致；`pnpm build` 成功；`max dev` + playwright-core 1.58.2（需手动指定 executablePath 到 `~/Library/Caches/ms-playwright/chromium_headless_shell-1234/...`，默认找 1208 不存在）冒烟 4 个页面渲染正常（仅有后端代理 504，与升级无关）。

### 明确不升级及原因
- `antd` 5→6：`@ant-design/pro-components` 稳定版 2.8.10 peer 仅支持 antd ^4/^5，antd 6 需 pro-components 3.x（仍 beta）；连带 `antd-style` 4（peer antd>=6）、`@ant-design/cssinjs` 2、`@ant-design/icons` 6 都依赖 antd 6，需作为一个整体迁移单独做。
- `@antv/g6` 4→5：API 全面重写（配置化 Graph），且 src 中无任何引用。
- `path-to-regexp` 6→8：v8 纯 ESM 且无 default 导出，破坏 `src/utils/index.ts`、`src/models/app.ts` 的默认导入；umi 内部也依赖 v6。
- `@babel/runtime` 7→8：需配套 Babel 8（@babel/core 8），umi 4 仍在 Babel 7。
- `react-router` 7→8：src 未直接引用，需与 react-router-dom 同步升，且 umi 4 内部用 react-router 6，混用风险高。
- `react-helmet-async` 1→3：Helmet 实际来自 `@umijs/max`，v2+ 移除默认导出，可能破坏 umi 内部解析。
- `react-cookie` 4→8、`react-google-recaptcha` 2→3、`react-dev-inspector` 1→2：代码未使用，收益低，暂不动。
- `typescript` 5.9→7：TS 7 原生端口，叠加 500+ 存量类型错误风险过高。
