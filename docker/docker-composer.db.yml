version: '3.9'

services:
  postgres:
    image: postgres:15
    ports:
      - '54322:5432'
    restart: always
    volumes:
      - db_data:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_HOST_AUTH_METHOD: trust

volumes:
  db_data:
